<div id="submission_media_select">
  <% if media.present? %>
    <h5><%= t('morphosource.submission.media_select') %></h5>
    <div class="form-group">
      <div class="form-inline">
        <%= f.input :parent_media_id, collection: media_selector, prompt: 'Select media',
                    input_html: {multiple: true, style: 'width: 400px'}, label: false %>

        <%#= f.select :parent_media_ids, 
           # options_for_select(media_selector), {}, 
            #{:multiple => true, :class => "multi_select", :style => "width:450px;" } %>

      </div>
      <div>
        <%= f.submit 'Continue with selected parent media', name: 'parent_media_select', class: 'btn btn-info', role: 'button' %>  
      </div>
    </div>
  <% end %>
</div>
