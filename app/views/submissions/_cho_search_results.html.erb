<div id="submission_cho_search_results">
  <div class="h4">
    <%= "Search Results" %>
  </div>
  <% if @docs.nil? || @docs.empty? %>
    <div id="empty_search_results">
      No Cultural Heritage Object found in MorphoSource that match the search criteria.  Search again or create a new Cultural Heritage Object.
    </div>
    <% else %>
      <div id="submission_select_cho">
        <%= f.input :cho_id,
                    as: :radio_buttons,
                    collection: @docs.map{ |doc| [ doc.sortable_title, doc.id ]} %>
      </div>
      <div>
        <%= f.submit 'Continue with selected Cultural Heritage Object', name: 'cho_select', class: 'btn btn-primary', onclick: 'return validateSelectedCho()'  %>
      </div>
      <div id="submission_choose_create_cho">
        <a href="#" id="submission_show_create_cho" class="btn btn-info">Create New Cultural Heritage Object</a>
      </div>
    <% end %>
</div>
