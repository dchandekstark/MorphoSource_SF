<div id="submission_parents_in_ms" class="derived_media hide">  
  
  <%= t('morphosource.submission.parents_in_ms') %>
  
  <div class="submission_flow" id="submission_select_parent_media">
    <%= simple_form_for @submission do |f| %>
      <%= render(partial: 'parent_media_select', locals: { f: f }) %>
    <% end %>
  </div>

  <%= f.submit 'Parents not in MorphoSource', id: 'btn_parents_not_in_morphosource', class: 'btn btn-primary' %>
</div>
