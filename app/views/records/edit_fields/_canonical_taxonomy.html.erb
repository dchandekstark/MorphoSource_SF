<% unless current_controller?('submissions') # no need to display this in submissions flow %>

	<%= render 'form_parent_work_relationships_taxonomy', f: f %>

  <div class="form-group">
  	<a class="btn btn-sm btn-primary" id="btn_new_taxonomy">Create new taxonomy</a>
	  <div id="embedded_div_new_taxonomy" class="embedded_div ui-loading" style="display: none;">
	  	<!-- new institution form will be loaded here -->
	  </div>
	</div>

<% end %>
