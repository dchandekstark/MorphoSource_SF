<!DOCTYPE html>
<html lang="<%= t("hyrax.document_language", default: '') %>" prefix="og:http://ogp.me/ns#">
  <head>

<%= csrf_meta_tag %>
<meta charset="utf-8" />

<!-- added for use on small devices like phones -->
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<link rel="resourcesync" href="<%= hyrax.capability_list_url %>" />

<title><%= content_for?(:page_title) ? yield(:page_title) : default_page_title %></title>

<!-- application css -->
<%= stylesheet_link_tag 'application' %>

<!-- application js -->
<%= javascript_include_tag 'application' %>

<%= render 'shared/appearance_styles' %>



    <%= content_for(:head) %>
  </head>

  <body>
    <div id="content-wrapper" class="container embedded-page" role="main">

        <a name="skip-to-content" id="skip-to-content"></a>
        <%= content_for?(:content) ? yield(:content) : yield %>

    </div><!-- /#content-wrapper -->
  </body>
</html>